{% extends 'base.html.twig' %}
{% block title %}Section d'affectation{% endblock %}
{% block body %}
    <div class="container mt-5">
        <h1>Assigner le ticket n°{{ticket.id}}</h1>
        {{form_start(form)}}
        <div class="form-group">
            <select class="custom-select" name="TECHNICIEN">
                {% for te in tech %} 
                    <option style="height:50px;" value={{te.id}}> {{te.firstname}} </option>
                {% endfor %}
            </select>
          </div>
        <button type="submit" class="btn btn-primary">Assigner</button>
        {{form_end(form)}}
    </div>
{% endblock %}